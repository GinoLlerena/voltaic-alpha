"""Read-only presentation API (`RUI-1`).

GET only, no credentials, no broker. It serves the same read models the
Streamlit dashboard renders, through public DTOs that allowlist what may cross
the boundary. See `docs/improvements/options_alpha_react_ui_redesign_v0_1.md`.
"""
